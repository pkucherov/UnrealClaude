---
phase: 01-test-baseline
plan: 05
type: execute
wave: 2
depends_on: [01-01, 01-02, 01-03, 01-04]
files_modified:
  - .planning/codebase/COVERAGE.md
autonomous: true
requirements: [TEST-00]
must_haves:
  truths:
    - "Coverage report documents method-level tracking for all components"
    - "Coverage gaps are identified with risk assessment for Phase 2 refactoring"
    - "MCP bridge JavaScript test status is documented (submodule not initialized)"
  artifacts:
    - path: ".planning/codebase/COVERAGE.md"
      provides: "Method-level coverage report with gap analysis and risk assessment"
      contains: "Coverage Report"
  key_links:
    - from: ".planning/codebase/COVERAGE.md"
      to: "All test files in Private/Tests/"
      via: "Documents which methods are tested by which test files"
      pattern: "Tests\\.cpp"
---

<objective>
Create the comprehensive test coverage report and document MCP bridge JavaScript test status.

Purpose: D-29 requires a full coverage report with method-level tracking, gap analysis, and risk assessment. D-30 places it in .planning/codebase/. D-04 requires documenting MCP bridge JS test status. This plan depends on all prior plans because it must account for all new test files created in Plans 01-04.
Output: .planning/codebase/COVERAGE.md (~300-400 lines)
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-test-baseline/01-CONTEXT.md
@.planning/phases/01-test-baseline/01-RESEARCH.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/01-test-baseline/01-01-SUMMARY.md
@.planning/phases/01-test-baseline/01-02-SUMMARY.md
@.planning/phases/01-test-baseline/01-03-SUMMARY.md
@.planning/phases/01-test-baseline/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create comprehensive coverage report</name>
  <files>.planning/codebase/COVERAGE.md</files>
  <action>
    Create `.planning/codebase/COVERAGE.md` per D-29, D-30.

    **Step 1 — Read all source headers to build the method inventory.**
    For each component, read its public/private header to enumerate all public methods:
    - `Public/ClaudeSubsystem.h` — FClaudeCodeSubsystem methods
    - `Public/ClaudeCodeRunner.h` — FClaudeCodeRunner methods
    - `Public/IClaudeRunner.h` — IClaudeRunner interface, FClaudeStreamEvent, FClaudeRequestConfig
    - `Public/ClaudeSessionManager.h` — FClaudeSessionManager methods
    - `Private/MCP/UnrealClaudeMCPServer.h` — FUnrealClaudeMCPServer methods
    - `Private/MCP/MCPToolRegistry.h` — FMCPToolRegistry, IMCPTool, FMCPToolInfo, FMCPToolAnnotations
    - `Private/MCP/MCPToolBase.h` — FMCPToolBase helper methods
    - `Private/ProjectContext.h` — FProjectContextManager, FProjectContext structs
    - `Public/ScriptExecutionManager.h` — FScriptExecutionManager methods
    - `Private/Widgets/SClaudeInputArea.h` — SClaudeInputArea public API
    - `Private/Widgets/SClaudeToolbar.h` — SClaudeToolbar public API
    - `Public/ClaudeEditorWidget.h` — SClaudeEditorWidget
    - `Public/UnrealClaudeConstants.h` — All constant namespaces
    - `Private/UnrealClaudeModule.cpp` — Module startup/shutdown (functions, not header)

    **Step 2 — Read all test files** (existing + new from SUMMARYs).
    For each test file, enumerate all IMPLEMENT_SIMPLE_AUTOMATION_TEST and IMPLEMENT_COMPLEX_AUTOMATION_TEST entries. Cross-reference which methods each test covers.

    Existing test files (8):
    - `MCPToolTests.cpp`, `MCPParamValidatorTests.cpp`, `MCPTaskQueueTests.cpp`
    - `CharacterToolTests.cpp`, `ClipboardImageTests.cpp`, `MaterialAssetToolTests.cpp`
    - `MCPAnimBlueprintTests.cpp`, `MCPAssetToolTests.cpp`

    New test files (from Plans 01-04, read SUMMARYs for actual counts):
    - `TestUtils.h`, `ClaudeSubsystemTests.cpp`, `ClaudeRunnerTests.cpp`
    - `SessionManagerTests.cpp`, `MCPServerTests.cpp`, `ConstantsTests.cpp`
    - `ProjectContextTests.cpp`, `ScriptExecutionTests.cpp`, `ModuleTests.cpp`
    - `SlateWidgetTests.cpp`, `MCPTaskQueueTests.cpp` (updated)

    **Step 3 — Write COVERAGE.md with this structure:**

    ```markdown
    # Test Coverage Report

    **Generated:** {date}
    **Phase:** 01 — Test Baseline
    **Status:** Post-Phase 1 baseline

    ## Summary

    | Metric | Count |
    |--------|-------|
    | Total test files | {N} (8 existing + {M} new) |
    | Total tests | {count} (171 existing + {new} new) |
    | Components with tests | {N} / {total} |
    | Disabled tests | 6 (MCPTaskQueue threading — see notes) |

    ## Component Coverage

    ### FClaudeCodeSubsystem
    **Test file:** ClaudeSubsystemTests.cpp
    **Coverage:** {percent estimate}

    | Method | Tested | Test Name | Notes |
    |--------|--------|-----------|-------|
    | Get() | ✅ | FClaudeSubsystem_Singleton_Access | |
    | GetRunner() | ✅ | ... | |
    | SendPrompt() | ❌ | — | Unsafe: spawns subprocess (Pitfall 9) |
    ...

    {Repeat for each component}

    ### MCP Tool Coverage (Existing)
    {Table of all 30+ tools with test status from MCPToolTests.cpp}

    ## MCP Bridge JavaScript Tests
    **Status:** Submodule not initialized in this clone
    **Expected:** npm test in Resources/mcp-bridge/
    **Test structure (from documentation):**
    - tests/unit/ — Schema conversion, HTTP client, context loader
    - tests/integration/ — Tool listing, execution with mocked server
    - tests/helpers/ — Mock fetch, fixtures
    **Action required:** Initialize submodule (`git submodule update --init`) and run `npm test` to verify

    ## Gap Analysis

    ### Untested Methods (Risk Assessment)

    | Component | Method | Risk | Reason Not Tested |
    |-----------|--------|------|-------------------|
    | FClaudeCodeSubsystem | SendPrompt | HIGH | Spawns subprocess, would need full integration test |
    | FClaudeCodeRunner | ExecuteAsync | HIGH | FRunnableThread deadlock in test context |
    ...

    ### Risk Assessment for Phase 2 Refactoring

    | Risk Level | Components | Mitigation |
    |------------|------------|------------|
    | LOW | {components with >90% safe method coverage} | Existing + new tests provide safety net |
    | MEDIUM | {components with partial coverage} | Key behavior tested, some paths untested |
    | HIGH | {components where critical paths are untestable} | Manual verification required during refactoring |

    ## Coverage by Decision

    | Decision | Status | Notes |
    |----------|--------|-------|
    | D-01 Full sweep | ✅ | All components have test files |
    | D-02 >=90% target | {status per component} | |
    ...
    ```

    **Use actual data from SUMMARYs** — read all 4 SUMMARYs to get real test counts, not estimates. If a SUMMARY doesn't exist yet (plan not executed), use the plan's target counts as estimates and mark with "~" prefix.

    **MCP bridge section (D-04):** Document that the submodule is not initialized. Note the expected test structure from TESTING.md. This satisfies D-04 by including JS tests in the baseline documentation, even though they can't be run in the current clone state.

    Target: 300-400 lines. If over 500, split tables into sub-sections.
  </action>
  <verify>
    <automated>MISSING — Verify by checking .planning/codebase/COVERAGE.md exists, contains "Component Coverage" section, contains "Gap Analysis" section, contains "MCP Bridge" section, is under 500 lines.</automated>
  </verify>
  <done>COVERAGE.md exists in .planning/codebase/ with method-level tracking for all components, MCP bridge JS test documentation, gap analysis with risk assessment, and coverage-by-decision tracking. All decisions D-01 through D-31 accounted for.</done>
</task>

</tasks>

<verification>
- COVERAGE.md is in .planning/codebase/ per D-30
- Report has method-level tracking per D-29
- Gap analysis includes risk assessment per D-29
- MCP bridge JavaScript tests documented per D-04
- All decisions D-01 through D-31 have coverage status
- File is under 500 lines (D-16 applies to documentation too)
</verification>

<success_criteria>
- .planning/codebase/COVERAGE.md exists with comprehensive method-level coverage data
- Every component from the plugin has a coverage section
- MCP bridge JS test status documented (submodule not initialized, expected structure noted)
- Gap analysis identifies untested critical paths with risk levels for Phase 2
- Coverage-by-decision table shows status for all 31 decisions
</success_criteria>

<output>
After completion, create `.planning/phases/01-test-baseline/01-05-SUMMARY.md`
</output>
